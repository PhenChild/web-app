import { NumberFormatStyle } from "@angular/common";

export class Estacion {
    codigo: string 
    nombreEstacion: string
    latitud: number
    longitud: number
    altitud: number
    suelo: string
    omm: string
    createdAt: string
    updatedAt: string
}